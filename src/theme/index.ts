import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1A5319',
    },
    secondary: {
        main: '#508D4E',
    },
    error: {
        main: '#FF2929',
    }
  },
});

export default theme; 